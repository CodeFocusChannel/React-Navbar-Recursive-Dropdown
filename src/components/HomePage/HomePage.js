import React from "react";
import { SHomePage, STitle } from "./styles";

const HomePage = () => {
    return (
        <SHomePage>
            <STitle>Home</STitle>
        </SHomePage>
    );
};

export default HomePage;
